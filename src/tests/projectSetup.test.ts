import { describe, expect, it } from "vitest";

describe("Neon Flow project setup", () => {
  it("runs TypeScript tests successfully", () => {
    const projectName = "Neon Flow";

    expect(projectName).toBe("Neon Flow");
  });
});